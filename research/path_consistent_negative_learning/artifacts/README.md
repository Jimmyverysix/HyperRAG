# 研究产物

- `audit/`：11 个领域的汇总、跨领域汇总和门槛 B 判定。
- `gate_c/`：20 组训练的运行清单、完成日志、聚合指标和门槛 C 判定。
- `figures/`：由汇总 JSON 直接生成的论文图（PDF 与 PNG）。
- `provenance.json`：数据来源、代码提交、随机种子、软件环境、硬件和服务器原始产物位置。

逐问题审计 JSONL、模型权重、逐问题测试指标和训练曲线体积较大，保留在 `provenance.json` 所列服务器目录，不提交到 GitHub。论文表格和图中的数字只读取本目录的聚合 JSON，不读取手工填写的数据。
